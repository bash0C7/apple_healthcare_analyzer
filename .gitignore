# Apple Health personal data — never commit
export.xml
export_cda.xml
electrocardiograms/
workout-routes/
data/
output/

# SQLite database (rebuild with load_db.rb)
db/*.db
db/*.db-shm
db/*.db-wal

# Local Claude Code settings (personal permissions)
.claude/settings.local.json

# Superpowers plans/specs (auto-generated, not for project repo)
docs/superpowers/

# macOS
.DS_Store
